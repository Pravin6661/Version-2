/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AddExamscheduleService } from './add-examschedule.service';

describe('Service: AddExamschedule', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AddExamscheduleService]
    });
  });

  it('should ...', inject([AddExamscheduleService], (service: AddExamscheduleService) => {
    expect(service).toBeTruthy();
  }));
});
