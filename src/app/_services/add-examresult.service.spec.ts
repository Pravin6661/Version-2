/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AddExamresultService } from './add-examresult.service';

describe('Service: AddExamresult', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AddExamresultService]
    });
  });

  it('should ...', inject([AddExamresultService], (service: AddExamresultService) => {
    expect(service).toBeTruthy();
  }));
});
